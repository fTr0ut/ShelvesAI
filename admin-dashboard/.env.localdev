# Local dev overrides — loaded only via `npm run dev:local`
# Overrides .env values when the localdev flag is active.
# Safe to commit — contains no secrets, only localhost URLs.
# For machine-specific overrides, create .env.localdev.local (gitignored).
API_TARGET=http://localhost:5001
VITE_API_URL=/api
