### FOR AGENTs
-You must update this file with your changes / task completions. 
-You must test all changes that you implement.
-You must update DependencyTree.md with your changes.

### Enhancements to prevent bugs
-~~Enhance the workflow in the enrichment phase to reply back to the existing Gemini request to keep the context level high and provide more accurate results. Right now, a new/separate request is sent to Gemini to cleanup up noisy ocr. It's given the results of the previous conversation, but context could help provide higher quality cleanup.~~ **DONE 3/23/26** — Enrichment now continues the original Gemini chat session via `startChat({ history })` + `sendMessage()`. The model retains the shelf photo and its own extraction when enriching. Applies to all shelf types including "other" (search grounding). Falls back to standalone mode when vision/text models differ or MLKit provides raw items.

~~Now that each request has a jobid, develop a work queue so we don't overload APIs. If a queue becomes long, keep the job in queue but notify the user that "we're working on it! We'll ping you when your request is complete". Send a notification to the user was the jobid reaches a pass/fail state. Think about adding a Global Rate limiter for inbound requests, Outbound Task Queue, and Aggressive caching to save Supabase(Postgres) and AWS S3 costs.~~ **DONE 3/30/26** - Implemented durable Postgres queueing for vision workflows (`workflow_queue_jobs`) with claim-safe worker execution, per-user queued cap + dedupe (`user+shelf+image hash`) and queue-aware async response/status metadata (`queuePosition`, `estimatedWaitSeconds`, `notifyOnComplete`). Added queue/running abort semantics, route-level ingress throttles on `/vision` and `/catalog-lookup`, shared outbound limiter registry for Gemini/catalog/S3 calls, S3 duplicate-scan hash short-circuit, queue-pressure crop warmup capping, and queue-only terminal push/notification support (`workflow_complete`/`workflow_failed`, `workflow_job`, `push_workflow_jobs` mobile toggle + navigation handling).
 
-~~Enhancement propose: A 'Replace' button should be used by the user when vision OCR erroneously matches OCR text and categorizes it as a high-confidence item, but is not actually what the user has or intended.~~ **DONE 3/25/26** - Implemented end-to-end replacement workflow: owner-only `CollectableDetailScreen` CTA (`Not the item you intended to add?`) for vision-linked items within 72h, `ShelfDetailScreen` delete interception modal for items within 24h (`Replace` / `Delete` / `Cancel`), `ItemSearchScreen` replacement mode (prefill + replace submit + return to shelf list), and backend trace/event plumbing via new `item_replacement_traces` table + RLS + query module with new endpoints `POST /api/shelves/:shelfId/items/:itemId/replacement-intent` and `POST /api/shelves/:shelfId/items/:itemId/replace` (intent/completion/failure tracking, transactional source->target swap, no notes/rating/photo transfer, no replacement feed events).

-~~Replicate the "add new shelf" button to the top of the My Shelves list when the list grows past 6 shelves.~~ **DONE 3/25/26** - `ShelvesScreen` now renders an additional top-of-list "New Shelf" CTA when the user has more than 6 shelves (while preserving the existing in-list create card).

-~~Surface fields filled in by the user on the ManualEditScreen to the CollectableDetailScreen.~~ **DONE 3/25/26** - `CollectableDetailScreen` now renders explicit manual-editable fields (`format`, `author`, `publisher`, `year`, `genre`, `edition`, `limited edition`, `age statement`, `label color`, `special markings`, `region`, `barcode`, `item details`) only when populated, always keeps personal notes in a dedicated section, and refreshes manual details on focus after returning from edit.

-~~Surface "Your Notes" directly on `CollectableDetailScreen` for quick review/edit access on both manual and collectable shelf items.~~ **DONE 3/25/26** - Added inline `Your Notes` editor on `CollectableDetailScreen` for owned shelf items (manual + collectable), reusing existing endpoints (`PUT /api/shelves/:shelfId/manual/:itemId` and `PUT /api/shelves/:shelfId/items/:itemId/rating`), and extended the collectable rating endpoint to accept notes-only updates without forcing rating changes. Follow-up hardening: collectable note saves now retry with resolved shelf-item id when the initial param is a collectable id, the UI now shows backend error text, backend `rateShelfItem` now accepts `collectableId` fallback to resolve the correct `user_collections.id` when detail-route item ids are mismatched, notes-only saves now also auto-treat `:itemId` as a collectable fallback candidate when explicit `collectableId` is missing, false `Item not found` modals are suppressed when server state confirms the note was persisted, the notes UX now matches requested view/edit behavior (pencil-to-edit when notes exist, editor+save when empty, and `Your Notes` rendered above item `Details`), and read-only/public friend shelf views now label both notes and owner rating with the shelf owner handle (e.g., `mrTr0ut's Notes:` / `mrTr0ut's rating:`).
-~~Add a `Share to feed?` toggle in notes textbox editor mode and create a new `Reviewed` feed event with item metadata + user rating.~~ **DONE 3/25/26** - Added `Share to feed?` toggle to `CollectableDetailScreen` notes editor mode (manual + collectable flows), extended existing save endpoints to accept `shareToFeed`, and introduced shelf-scoped `reviewed` feed events emitted only when notes are saved as non-empty with sharing enabled. `reviewed` payloads now include item identity, notes, metadata snapshot fields, and current decoupled user rating; mobile feed/event detail UIs now render reviewed activity with notes + rating context.

-~~When shelf = 'other', the AI progress modal window only stays at 10% the entire process. Flesh out the progress status updates further to make the process seem less stale.~~ **DONE 3/25/26** - Added dedicated `other`-workflow progress stages in `visionProgressMessages.json` (`extractingSecondPass`, `matchingOther`, `reviewingOther`) and updated `VisionPipelineService` to use monotonic, `other`-specific status updates (including second-pass extraction), preventing long-running `other` scans from appearing stuck at 10%.

-~~Vision scan modal appears stalled at 10% for large-item photos, then jumps to 50% once parsing returns. Add intermediate progress states between 10% and 50% during extraction.~~ **DONE 4/1/26** - Added backend extraction heartbeat progress stages in `api/config/visionProgressMessages.json` (`extractingInFlight` at 20%, `extractingDeepParse` at 30%) and wired `VisionPipelineService.processImage()` to schedule/clear timed heartbeat updates (3s/9s) only while OCR extraction is still running. Kept mobile modal rendering unchanged, preserved queue/status endpoint contracts, and added regression coverage in `api/__tests__/visionPipeline.test.js` for both long-running extraction heartbeat emission and fast-extraction no-heartbeat behavior.


-~~Add a submit Feedback button in the Account Settings that provides a prompt for the user to fill out. Pass the details via Resend to support@shelvesai.com.~~ **DONE 3/25/26** 

-~~website/ enhancment - Add a Terms of Service agreement page.~~ **DONE 3/30/26**
-~~App enhancement - Add terms of service to onboarding process. Expand user schema to include whether it's been accepted or not, version of the terms, and the date of acceptance. Terms are being created and stored at shelvesai.com/terms~~ **DONE 3/30/26** - Added onboarding Terms section in mobile with required acceptance checkbox + Terms link to `https://shelvesai.com/terms`, enforced terms acceptance/version on `POST /api/onboarding/complete`, and persisted `users.terms_accepted`, `users.terms_accepted_version`, and `users.terms_accepted_at` with migration `20260330180000_add_users_terms_acceptance_fields`.
-Onboarding enhancement - Add a `Back to Login` option on the Terms acceptance screen so users can exit onboarding and return to the login page. **DONE 3/30/26** - `OnboardingProfileOptionalScreen` now includes `Back to Login` action that clears token/session state and routes users back to Login flow.

-~~The check-in functionality is only limited to items in the database when searching. The check-in functionality should call the GlobalSearch functions (search db and then API) to keep the experience unified.~~ **DONE 3/30/26** - Unified check-in search with global search fallback behavior. `GET /api/checkin/search` now supports the same API-fallback flow used by global collectables search (container resolution + cached provider lookup) while preserving manual-item results. `CheckInScreen` now requests fallback-enabled check-in search, includes an inline type filter in the search bar, exposes `See more results` to rerun with API supplement + higher limit, resolves API-backed results through `POST /api/collectables/resolve-search-hit` before `POST /api/checkin`, and emits a parent refresh signal so `SocialFeedScreen` refreshes immediately without mounting a new feed route.


### General Enhancements
-Behavior change: People will add all sorts of collectabls and manuals to a single shelf. Right now, if I add a movie that is classified as a shelf = 'book' type, the event card will say "Johnny added a new book to My First Shelf", even though the collectable is a movie. Adjust the event card to reference the collectable type first, fallback to shelf type when it's a manual add. iOS v1 build 3 3/29/26

-Add deep-linking to Wishlists and a Share button visible to owners of the wishlist. iOS v1 build 3 3/29/26

-Add ability to login using email associated to username.

-Add search by cast members as an option to GlobalSearch.

### Bugs/issues reported by users

-~~Feed showed duplicate rating activity entries in event system (repeated same-star ratings, occasional duplicate aggregates, and rating reused across multiple check-ins). 3/25/26~~ **DONE 3/25/26** - Implemented backend event-system dedupe for ratings: rating writes are now idempotent (`changed`/`previousRating`/`currentRating` contract), both `/api/ratings/:itemId` and legacy `/api/shelves/:shelfId/items/:itemId/rating` only emit `item.rated` when rating value changes, `feed.logEvent` now enforces one rating log per item per open aggregate (latest value wins) and uses advisory transaction locks to prevent same-scope concurrent aggregate duplication, and check-in/rating merge now consumes each rating item at most once so one rating cannot attach to multiple check-ins.
-~~iPhone: could not enter city / state without closing and reopening the app. v1 build 2 3/22/2026~~ **DONE 3/24/26** - Mitigated iOS text-entry lockups by hardening onboarding/profile form hydration (prevent async `/api/account`/`/api/profile` field re-hydration from overwriting active edits), adding stable iOS keyboard input settings for city/state fields, and chaining explicit next/done focus behavior.
-~~iPhpne: took photo of shelf several times, AI/Vision workflow completed but 0 items were added. Note from CREATOR: Possible reason could be token limit was reached. We should ensure that we still capture the json and parse it, even if its partial. v1 build 2 3/22/26~~ **DONE 3/23/26** - `detectShelfItemsFromImage()` no longer swallows Gemini transport failures as empty results (now throws `VISION_PROVIDER_UNAVAILABLE`/`VISION_EXTRACTION_FAILED` so the job fails visibly), and now attempts truncated JSON recovery for partial vision responses so complete items are still parsed/saved when token output is cut off.
-~~iphone: uploaded same photo to same 'other' shelf multiple times. Slight difference in how model returned name and creator, thus dupe were created each time. v1 build 2 3/22/26~~ **DONE 3/23/26** - Added `other` shelf duplicate controls across pipeline + review completion: canonicalized matching keys, exact fingerprint/barcode checks, conservative fuzzy matching (`fuzzy_auto` vs `fuzzy_review`), and in-scan batch dedupe (`barcode` -> `manualFingerprint` -> canonical title+creator).
-~~created 'other' shelf but used it for uploading books. Description was optional, so no text was inputted. Model did not know how to respond. Make Description mandatory when shelf = 'other'. v1 build 2 3/22/26.~~ **DONE 3/25/26** - Enforced `other` shelf description requirement end-to-end: mobile create/edit screens now require non-empty description for `other`, API create/update endpoints reject blank descriptions for `other`, and vision/catalog workflow endpoints now block scans/lookups for legacy `other` shelves missing descriptions.
-~~'Other' shelf easily allows duplicates due to slight various in the way that models return ocr data. 3/22/26~~ **DONE 3/23/26** - Added DB-level guardrails (`idx_user_collections_unique_manual`, `idx_user_manuals_unique_fingerprint`) with cleanup migration to collapse legacy duplicates and repoint dependent `manual_id` references before applying unique indexes.
-~~Backend Postgress pooler leak "[req_8a3f97d7] 2026-03-22T23:19:44.421Z [user:f4af6e7d-9df1-4ab3-9921-7aec00b228f9] error  Query error:  {"text":"INSERT INTO job_events (job_id, level, message, user_id, metadata, created_at)\n     SELECT $1, $2, $","error":"MaxClientsInSessionMode: max clients reached - in Session mode max clients are limited to pool_size"}" | 3/22/26~~ **DONE 3/23/26** - Disabled request-level HTTP logger middleware (`api/middleware/requestLogger`) in `api/server.js`, which removes GET/POST request console logs and request-driven `job_runs` / `job_events` DB writes.

-~~ShelfDetailList needs caching. When the user goes from ShelfDetailScreen --> CollectableDetailScreen --> shelfdetailScreen, the list flickers as if it's being destoryed and recreated. Interesting observation on iPhone (probably android as well), if the user explicitly uses the back button in the top lefthand corner then the list still cached/no flicker. If the user uses the built-in iOS swipe navigation to go to the previous screen, then it flickers/reloads. v1 build 2 3/23/206~~ **DONE 3/25/26** - `ShelfDetailScreen` now uses cached-first reload behavior: first load remains blocking, focus reloads are non-blocking and deferred until transitions/gestures complete (`useFocusEffect` + `InteractionManager.runAfterInteractions`), and concurrent reloads are deduped to prevent swipe-back flicker/rebuild artifacts.

-~~User searched for item that had an accent in the title (Pokemon/Pokemon with accent) but returned no results when searched with plain english letters. We should normalize searches or expand matching logic. v1 build 2 3/23/206~~ **DONE 3/25/26** - Implemented accent-insensitive hybrid search matching (raw + normalized OR branches) across item search endpoints (`/api/shelves/:shelfId/manual/search`, `/api/checkin/search`, `/api/collectables`) and local item filters (`ShelfDetailScreen`, `WishlistScreen`), plus fixed `ListDetailScreen` search path to `/api/collectables`. Added backend coverage for normalized query construction (`fuzzyMatching.test.js`, `checkinSearchQuery.test.js`).

-~~Server reported a save error when scanning a vinyl collection: "[wf_vision_5fac1594e1]~~ **DONE 3/23/26**

-~~"Add to shelf" workflow doesn't end after adding items to a shelf. A user can add items to their shelf, save, and when they hit the back button there are asked to "choose shelf" again.~~ **DONE 3/25/26** - Updated `ShelfSelectScreen` to `navigation.replace()` into `ShelfDetail` and `ShelfCreateScreen` during add-flow so `Choose Shelf` is removed from the back stack once the destination shelf workflow starts.

-~~Gemini's bounding box coordinates need pixel padding horizontally. Most crops are too close up and cut off some of the object. 3/23/26~~ **DONE 3/24/26** - Added env-tunable bbox pixel padding before scan-region persistence (`VISION_BBOX_PADDING_X_PX`, `VISION_BBOX_PADDING_Y_PX`) so future Gemini region crops include more object real estate. Also updated owner-photo thumbnail auto-generation for `vision_crop` sources to default to a centered 2/3 selection when no explicit thumbnail box is set; explicit thumbnail boxes and upload-source defaults remain unchanged.

-~~iOS reported a hang on the mobile app, tailspin log provided. 3/23/26~~ **DONE 3/24/26** - Tailspin indicated `UIKit-runloop` + keyboard-management dependency stalls on main thread. Added mitigation by removing the global iOS `KeyboardAvoidingView` wrapper around the app navigator and applying per-screen keyboard-stability fixes for profile/onboarding text-entry flows.
-iOS follow-up hang mitigation for build 4 text-input freezes. **DONE 4/1/26** - Added shared mobile text input policy (`mobile/src/utils/textInputPolicy.js`) to disable iOS autofill/prediction on non-auth fields while preserving explicit credential semantics (`username`, `email`, `password`, `newPassword`) for auth flows. Applied policy to high-traffic non-auth inputs (`GlobalSearchBar`, `FriendSearchScreen`, profile/onboarding edit forms, `AccountScreen` feedback modal, `QuickCheckInModal`) and auth screens (`LoginScreen`, `ForgotPasswordScreen`, `ResetPasswordScreen`). Replaced feedback modal `autoFocus` with deferred `InteractionManager.runAfterInteractions` focus + cancellation cleanup to reduce keyboard task queue contention during modal open/close transitions. Added regression coverage in `mobile/src/utils/textInputPolicy.test.js`.

-~~Foreign users have access to a owner's Wishlist, which is intended. However, they have the delete icon visible on their screen. They receive an error message when attempting to delete said item because of token auth, but the icon needs to be hidden. iOS v1 build 3 03/28/26~~ **DONE 3/30/26** - API `getWishlist` now returns `isOwner` boolean; `WishlistScreen` conditionally renders the trash icon and add-item FAB only for owners, and shows appropriate empty-state text for non-owners.

-~~When copying a Share link to the device's clipboard, two of the same link are copied in. iOS v1 build 3 03/28/26~~ **DONE 3/30/26** - Fixed duplicate share URL on iOS by using platform-aware `Share.share()` parameters: iOS now passes `url` separately (iOS appends it automatically), Android includes URL in `message` body (Android ignores `url` param).

-~~Possible bug: it appears that in some instances, multiple push notifications are being sent out for one push event (like on an event card). iOS v1 build 3 3/29/26~~ **DONE 3/30/26** - Fixed duplicate pushes by making push token registration transactional with per-installation stale-token deactivation (plus opportunistic legacy-token retirement), switching mobile `deviceId` to persisted `install:<uuid>`, and adding DB/query-level notification dedup for `workflow_complete`/`workflow_failed` and `friend_accept`.

-~~Platform doesn't seem to be mapped to a collectable field from the Games upsert. iOS v1 build 3 3/29/26~~ **DONE 3/31/26** - Added end-to-end IGDB platform capture and ownership UX: `collectables.platform_data` now persists normalized IGDB platform metadata (including release-date platform rows), game search endpoints now enforce strict local platform filtering when `platform=` is provided, shelf items now carry `ownedPlatforms`, and owners can edit per-item platform chips via `PUT /api/shelves/:shelfId/items/:itemId/platforms`. Game add flows (`addCollectable`, `addCollectableFromApi`, review completion, vision saves) now auto-seed a default owned platform from `systemName` when present.

-~~GlobalSearch doesn't appear to recongize TV API for searching. iOS v1 build 3 3/29/26~~ **DONE 3/30/26** - Fixed API-container TV fallback wiring by including shared `TvCatalogService` in `CollectableMatchingService` catalog resolution, so typed/global searches using container `tv` now call TMDB TV lookup instead of short-circuiting with no provider.

-~~Deleting a shelf in the Shelf's settings will delete it, but the screen does not update and it does not navigate the user back to their main Shelves page. We also need to add in a confirmation modal. iOS v1 build 4 3/30/2026~~ **DONE 4/1/26** - `ShelfEditScreen` now keeps delete confirmation, waits for successful `DELETE /api/shelves/:shelfId`, then resets navigation to `Main -> Shelves -> ShelvesHome` so users always return to their Shelves list after deletion (fixes stale nested `ShelfEdit` stack state).

-~~shelvesController only allows a specified range of shelves to be displayed, with a limit of 100. This limit needs to be lifted or refractored smartly. iOS v1 build 4 3/30/2026~~ **DONE 4/1/26** - Implemented paginated + sortable shelves listing (`GET /api/shelves` now supports `limit`, `skip`, `sortBy`, `sortDir` with deterministic ordering and response `sort` metadata), added conditional shelf-list caching via ETag/304 (`Cache-Control: private, max-age=0, must-revalidate`), and updated mobile shelves consumers to load all pages as needed (`ShelvesScreen` infinite-scroll browse mode with sort modal + search-mode sort disable, plus paged-all fetches for `ShelfSelectScreen` and `AddToShelfModal`).

-~~User reported getting an "admin access denied" error when attempting to check-in on an item and create a new event on their feed.~~ **DONE 4/1/26** - `POST /api/collectables/from-news` had `requireAdmin` middleware incorrectly applied (copy-paste from the admin-only `POST /api/collectables` catalog-write route above it), blocking non-admin users from the `QuickCheckInModal` news-item check-in flow. Removed `requireAdmin` from the route; standard `auth` middleware (applied at the router level) still enforces authentication.



### Cleanup and Changelog
-Owned Platforms format requirement + badge UX. **DONE 3/31/26** - `CollectableDetailScreen` now requires selecting `Physical` or `Digital` before saving Owned Platforms, disables save until selected, and renders the selected ownership format as a badge on each owned-platform row after save. Backend `PUT /api/shelves/:shelfId/items/:itemId/platforms` now enforces `format` as required (`physical|digital`) for game collectables.
-CollectableDetailScreen game metadata/owned-platform UX refinement. **DONE 3/31/26** - For game detail views, changed `Max Players` label to `# of Players`, suppresses `IGDB Rating` in Details when value is `0.0`, and extended owned-platform save flow to include ownership format selection (`Physical` / `Digital`) persisted via `PUT /api/shelves/:shelfId/items/:itemId/platforms` into collectable format metadata.
-Catalog timeout false-abort hardening. **DONE 3/31/26** - Fixed outbound catalog timeout behavior so request timeouts start only when the HTTP request actually begins (not while waiting in local rate-limit/outbound queues). Updated `api/services/openLibrary.js` and `api/services/hardcover.js` to create `AbortController`/timeout inside the limiter-executed task, preventing queue-wait time from triggering `"The user aborted a request."` during high-throughput vision scans.
-~~every post/get is assigned a jobid. That doesn't seem necessary. We should only assign jobids to workflow request items.~~ **DONE 3/23/26** - Request middleware unmounted; workflow/scheduled job logging via `jobRunner` remains available.
-~~Re-add request-level auto job IDs for workflow endpoints only (vision/catalog/etc) after global request logger removal.~~ **DONE 3/23/26** - Added lightweight workflow context middleware (`api/middleware/workflowJobContext.js`) and mounted it on `POST /api/shelves/:shelfId/vision` and `POST /api/shelves/:shelfId/catalog-lookup` to auto-assign request job IDs without re-enabling global GET/POST request DB logging.
-Added market value enhancement end-to-end (3/23/26): introduced `market_value` on `collectables` + `user_manuals`, added `market_value_sources` JSONB storage for Gemini links, wired persistence through vision/manual/catalog flows, and updated Gemini prompts/schema to request both value and sources. API payloads currently omit `marketValueSources` intentionally.
-Added vision match observability (3/23/26): when extracted items resolve to existing records, `VisionPipeline` now logs `sourceTable` + `sourceId` for `collectables` and `user_manuals` matches (including `user_collections` id when a manual match is already linked or newly linked to a shelf).
-Added same-photo vision idempotency for shelf scans (3/23/26): introduced persistent `vision_result_cache` keyed by `user_id+shelf_id+image_sha256` with 24h TTL. `POST /api/shelves/:shelfId/vision` now short-circuits on cache hit (sync + async) and logs cache hit/miss; successful uncached runs persist cache entries.
-Vision pipeline stabilization updates (3/24/26): fixed `collectables.upsert` JSON/text parameter ordering that caused `Token "USD" is invalid` save failures, added Gemini confidence patch retry (single follow-up turn) for extraction rows missing confidence, added explicit OCR-vs-enrichment stage logs, and changed `saveToShelf` to route failed saves into `needs_review` with `reason: save_error` instead of silently dropping them.
-Vinyl enrichment prompt fix (3/24/26): corrected Gemini enrichment category resolution so `vinyl` shelves map to music-specific enrichment instructions instead of falling back to book defaults; also updated schema hint text to use category-aware identifier/format examples (e.g., UPC/Discogs/MusicBrainz + Vinyl LP terms).
-Implemented rollback-friendly persistent footer navigation for Shelves detail flows. **DONE 3/26/26** - `BottomTabNavigator` now supports `ENABLE_PERSISTENT_SHELVES_DETAIL_FOOTER` (single-line kill switch) and, when enabled, mounts a nested Shelves stack (`ShelvesHome`, `ShelfCreateScreen`, `ShelfSelect`, `ShelfDetail`, `ShelfEdit`, `ItemSearch`, `CollectableDetail`) so the tab footer stays visible through shelf/detail routes opened from the Shelves tab. Add-FAB now routes to nested `Shelves -> ShelfSelect` in this mode, while legacy root routing remains when disabled. Added tab-parent-aware bottom spacing in `ShelfDetailScreen` and `CollectableDetailScreen` so content/FABs clear the persistent footer.
-Feed added-event visual refresh. **DONE 3/26/26** - Reworked posting visuals across `SocialFeedScreen`, `ProfileScreen`, and `FeedDetailScreen` so added-event headers now use shelf-type dynamic grammar (no item title in header), shelf description is removed from added-event card bodies, single-item events show thumbnail + name/creator/year, and multi-item events keep thumbnail stacking. Added shared helper `mobile/src/utils/feedAddedEvent.js` and owner-photo thumbnail support for `other` shelves with placeholder fallback when personal-photo thumbs are unavailable. Backend now standardizes add-event payloads (`item.collectable_added` / `item.manual_added`) across shelf/manual/review/vision emitters and exposes normalized `title`/`creator`/`year` in feed item mapping (`GET /api/feed`, `GET /api/feed/:id`), including collectable year in feed-detail hydration.
-Admin dashboard workfeed monitor. **DONE 3/30/26** - Added live queue workfeed to admin Jobs area with default Workfeed tab (`queued` + `processing` active-first), 5s polling/refresh controls, and read-only detail modal for queue payload/result/error/progress. Backend now exposes `GET /api/admin/workfeed` and `GET /api/admin/workfeed/:jobId` with filtering, pagination, queue-position derivation, queued-time derivation, and in-memory progress snapshot enrichment for single-instance visibility.

